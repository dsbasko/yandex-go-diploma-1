version: '3'

services:
  planner:
    restart: "no"
    build:
      context: ../../../../
      dockerfile: ./services/planner/deploy/docker/dev.Dockerfile
    volumes:
      - ../../:/app
      - ../../../../core:/core
      - ../../../auth:/services/auth

  planner_db:
    build:
      context: ../../
      dockerfile: ./deploy/docker/psql.Dockerfile
    ports:
      - "${PLANNER_PSQL_PORT}:${PLANNER_PSQL_PORT}"