version: '3'

services:
  planner:
    restart: "no"
    build:
      context: ../../../../
      dockerfile: ./services/planner/deploy/docker/dev.Dockerfile
    volumes:
      - ../../:/app
      - ../../../../core:/core
