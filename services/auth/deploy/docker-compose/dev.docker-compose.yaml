version: '3'

services:
  auth:
    restart: "no"
    build:
      context: ../../../../
      dockerfile: ./services/auth/deploy/docker/dev.Dockerfile
    volumes:
      - ../../:/app
      - ../../../../core:/core
